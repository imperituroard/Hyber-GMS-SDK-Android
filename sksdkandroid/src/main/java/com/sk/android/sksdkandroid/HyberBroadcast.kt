package com.sk.android.sksdkandroid

import android.content.BroadcastReceiver
import android.content.Context
import android.content.Intent

internal class HyberBroadcast : BroadcastReceiver() {

    override fun onReceive(context: Context, intent: Intent) {
        //val serviceLauncher = Intent(context, MyService3::class.java)
        //val serviceLauncher = Intent(context, applicationContext::class.java)
        //context.startService(serviceLauncher)
    }

}